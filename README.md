# escala-almoco
Smart lunch group scheduler that intelligently distributes people across meal slots while maximizing area diversity and minimizing repeat pairings. Uses penalty-based algorithm to track pairing history and form balanced, diverse groups. All data stored locally.
